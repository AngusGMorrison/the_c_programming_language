#include <stdio.h>

/* Here is a file implementing standard C comments to be removed. */

int main() {
    /* "Quoted strings" should not cause any trouble for the removing
       program. */
    printf("Still working!");
    return 0;
}